{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "stm32f446re_example_build",
            "type": "shell",
            "command": "cd stm32f446re_example && cargo build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$rustc"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}
